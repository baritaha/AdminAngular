import { InjectionToken } from "@angular/core";
import Logger from "../../../models/logger.model";

export const LOGGER_TOKEN=new InjectionToken<Logger>('logger');
export const LOGGER_TOKEN2=new InjectionToken<Logger>('logger');
